<?xml version="1.0" encoding="utf-8" ?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:startDestination="@id/mainFragment">
    <fragment
        android:id="@+id/mainFragment"
        android:name="jp.tinyport.armyknife.MainFragment"
        android:label="MainFragment">
        <action
            android:id="@+id/action_mainFragment_to_keyStoreActivity"
            app:destination="@id/keyStoreActivity" />
    </fragment>
    <activity
        android:id="@+id/keyStoreActivity"
        android:name="jp.tinyport.armyknife.feature.keystore.KeyStoreActivity"
        android:label="KeyStoreActivity" />
</navigation>
